tickets_dir: ~/org/tickets
default_agent: claude

agents:
  claude:
    binary: claude
  programmator:
    binary: programmator
    effort: high

stages:
  commit:
    prompt: Commit the work.

pipelines:
  default:
    - stage: commit
      agent: claude
      on_success: done
      on_failure: pause
