tickets_dir: ~/org/tickets
default_agent: pi

agents:
  pi:
    binary: pi
    checkpoint_compaction_tokens: -1

stages:
  code:
    prompt: Write code.

pipelines:
  default:
    - stage: code
      agent: pi
      on_success: done
      on_failure: pause
