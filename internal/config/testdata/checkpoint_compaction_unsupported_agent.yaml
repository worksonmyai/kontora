tickets_dir: ~/org/tickets
default_agent: codex

agents:
  codex:
    binary: codex
    checkpoint_compaction_tokens: 10000

stages:
  code:
    prompt: Write code.

pipelines:
  default:
    - stage: code
      agent: codex
      on_success: done
      on_failure: pause
