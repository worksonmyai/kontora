tickets_dir: ~/org/tickets

agents:
  claude:
    args: ["--model", "sonnet"]

stages:
  code:
    prompt: Write code.

pipelines:
  default:
    - stage: code
      agent: claude
      on_success: done
      on_failure: pause
