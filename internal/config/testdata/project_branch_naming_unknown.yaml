agents:
  claude:
    binary: claude

stages:
  code:
    prompt: Write code.

pipelines:
  default:
    - stage: code
      agent: claude
      on_success: done
      on_failure: pause

projects:
  api:
    path: /repos/api
    branch_naming:
      mode: automatic
