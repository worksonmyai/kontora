tickets_dir: ~/org/tickets

default_agent: none

agents:
  none:
    binary: claude

stages:
  code:
    prompt: Write code.

pipelines:
  implement:
    - stage: code
      agent: none
      on_success: done
      on_failure: pause

projects:
  kontora:
    path: ~/projects/kontora
    agent: none
