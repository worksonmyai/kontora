tickets_dir: ~/org/tickets

agents:
  claude:
    binary: claude

stages:
  commit:
    prompt: Commit the work.
    model:
      unknown-agent: haiku

pipelines:
  default:
    - stage: commit
      agent: claude
      on_success: done
      on_failure: pause
