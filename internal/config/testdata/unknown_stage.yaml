tickets_dir: ~/org/tickets

agents:
  claude:
    binary: claude

stages:
  code:
    prompt: Write code.

pipelines:
  default:
    - stage: nonexistent
      agent: claude
      on_success: done
      on_failure: pause
